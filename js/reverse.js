"use strict"

/*
galo i prieki.
*/

const text = 'abcdef';
let reverseText = '';

/*
for (let i = 0; i < text.length; i++) {
    const letter = text[i];

    reverseText += letter + reverseText;

    console.log(i, letter, reverseText);
}

console.log('Final gavau:', reverseText);
*/
